// 模版类型
export enum E_TemplateType {
  PAGE = 'page',
  COMPONENT = 'component',
}

// 组件类型
export enum E_ComponentType {
  // 普通组件
  NORMAL = 'normal',
  // 表单组件
  DRAWER_FORM = 'drawerForm',
  MODAL_FORM = 'modalForm',
}
